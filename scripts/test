#!/usr/bin/env bash
set -euo pipefail

root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${root}"

uv run --frozen --group test pytest
./scripts/test-nvim --headless '+luafile tests/nvim/stage1_spec.lua'

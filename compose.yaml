services:
  test:
    build:
      context: .
      args:
        NVIM_VERSION: v0.12.5
    environment:
      HOME: /tmp/nvjup-home
      NVJUP_TEST_RUNTIME: /tmp/nvjup-runtime
    command: ["./scripts/test"]
